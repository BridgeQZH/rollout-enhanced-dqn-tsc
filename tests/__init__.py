"""Regression oracle for the rollout-enhanced TLCS refactors.

These tests are dependency-free (no pytest required): each module exposes
``test_*`` functions and a ``main()`` that runs them, printing PASS/FAIL and
exiting non-zero on failure — matching the project's standalone-script style.
Run the whole suite from the repository root with::

    python -m tests.run_all
"""
